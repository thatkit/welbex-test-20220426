/// <reference types="react" />
import { BlogNote as BlogNoteType } from '../../../../types';
export declare const BlogNote: (({ blogNote }: {
    blogNote: BlogNoteType;
}) => JSX.Element) & {
    displayName: string;
};
